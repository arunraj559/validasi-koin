<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>VALIDASI KOIN TVTOTO</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Rajdhani', 'Orbitron', sans-serif;
            background: linear-gradient(135deg, #0a0014 0%, #1a0033 25%, #0d001f 50%, #1f0040 75%, #0a0014 100%);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
            color: #00ffcc;
            min-height: 100vh;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 255, 204, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(138, 43, 226, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, rgba(0, 200, 255, 0.05) 0%, transparent 60%);
            pointer-events: none;
            z-index: 0;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .container {
            display: flex;
            gap: 30px;
            max-width: 1800px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        .main {
            flex: 1;
            background: rgba(10, 10, 30, 0.7);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            padding: 35px;
            border: 2px solid rgba(0, 255, 204, 0.3);
            box-shadow: 
                0 8px 32px rgba(0, 0, 0, 0.5),
                0 0 60px rgba(0, 255, 204, 0.15),
                inset 0 0 40px rgba(0, 255, 204, 0.05);
            transform-style: preserve-3d;
            transition: transform 0.3s ease;
        }

        .main:hover {
            transform: translateY(-5px);
            box-shadow: 
                0 15px 50px rgba(0, 0, 0, 0.6),
                0 0 80px rgba(0, 255, 204, 0.25),
                inset 0 0 50px rgba(0, 255, 204, 0.08);
        }

        h1 {
            text-align: center;
            color: #00ffcc;
            font-family: 'Orbitron', sans-serif;
            font-weight: 900;
            font-size: 2.8em;
            letter-spacing: 4px;
            margin-bottom: 30px;
            text-shadow: 
                0 0 10px rgba(0, 255, 204, 0.8),
                0 0 20px rgba(0, 255, 204, 0.6),
                0 0 30px rgba(0, 255, 204, 0.4),
                0 5px 15px rgba(0, 0, 0, 0.5);
            background: linear-gradient(135deg, #00ffcc, #00c9ff, #00ffcc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: titlePulse 3s ease-in-out infinite;
        }

        @keyframes titlePulse {
            0%, 100% { text-shadow: 0 0 10px rgba(0, 255, 204, 0.8), 0 0 20px rgba(0, 255, 204, 0.6); }
            50% { text-shadow: 0 0 20px rgba(0, 255, 204, 1), 0 0 40px rgba(0, 255, 204, 0.8); }
        }

        h2, h3 {
            color: #00ffcc;
            font-family: 'Orbitron', sans-serif;
            font-weight: 700;
            letter-spacing: 2px;
            margin: 20px 0 15px 0;
            text-shadow: 0 0 15px rgba(0, 255, 204, 0.6);
        }

        h2 {
            text-align: center;
            font-size: 1.8em;
        }

        h3 {
            font-size: 1.3em;
        }

        textarea {
            width: 100%;
            height: 140px;
            background: rgba(5, 5, 20, 0.8);
            backdrop-filter: blur(10px);
            color: #00ffcc;
            border: 2px solid rgba(0, 255, 204, 0.4);
            border-radius: 15px;
            padding: 15px;
            margin-bottom: 20px;
            resize: vertical;
            font-size: 15px;
            font-family: 'Rajdhani', monospace;
            box-shadow: 
                inset 0 0 20px rgba(0, 255, 204, 0.1),
                0 5px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }

        textarea:focus {
            outline: none;
            border-color: #00ffcc;
            box-shadow: 
                inset 0 0 30px rgba(0, 255, 204, 0.2),
                0 0 25px rgba(0, 255, 204, 0.4),
                0 8px 20px rgba(0, 0, 0, 0.4);
            transform: translateY(-2px);
        }

        textarea::placeholder {
            color: rgba(0, 255, 204, 0.4);
        }

        .button-group {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-bottom: 25px;
            justify-content: center;
        }

        button {
            background: linear-gradient(135deg, rgba(0, 255, 204, 0.9), rgba(0, 200, 255, 0.9));
            color: #0a0014;
            border: none;
            padding: 14px 28px;
            border-radius: 12px;
            cursor: pointer;
            font-weight: 700;
            font-family: 'Orbitron', sans-serif;
            letter-spacing: 1.5px;
            font-size: 14px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 
                0 5px 15px rgba(0, 255, 204, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            position: relative;
            overflow: hidden;
        }

        button::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        button:hover::before {
            width: 300px;
            height: 300px;
        }

        button:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 
                0 10px 30px rgba(0, 255, 204, 0.5),
                0 0 40px rgba(0, 255, 204, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.4);
        }

        button:active {
            transform: translateY(-1px) scale(1.02);
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin-top: 20px;
            background: rgba(5, 5, 20, 0.7);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 
                0 8px 32px rgba(0, 0, 0, 0.4),
                0 0 30px rgba(0, 255, 204, 0.1);
        }

        th, td {
            border: 1px solid rgba(0, 255, 204, 0.3);
            padding: 14px;
            text-align: center;
            font-size: 15px;
            font-weight: 500;
        }

        th {
            background: linear-gradient(135deg, rgba(0, 255, 204, 0.2), rgba(0, 200, 255, 0.2));
            color: #00ffcc;
            font-weight: 700;
            font-family: 'Orbitron', sans-serif;
            letter-spacing: 1px;
            text-shadow: 0 0 10px rgba(0, 255, 204, 0.5);
        }

        tr {
            transition: all 0.3s ease;
        }

        tr:hover {
            background: rgba(0, 255, 204, 0.1);
            transform: scale(1.01);
        }

        .sidebar {
            width: 500px;
            background: rgba(10, 10, 30, 0.7);
            backdrop-filter: blur(20px);
            border: 2px solid rgba(0, 255, 204, 0.3);
            padding: 30px;
            border-radius: 25px;
            box-shadow: 
                0 8px 32px rgba(0, 0, 0, 0.5),
                0 0 60px rgba(0, 255, 204, 0.15),
                inset 0 0 40px rgba(0, 255, 204, 0.05);
            display: flex;
            flex-direction: column;
            gap: 20px;
            transform-style: preserve-3d;
            transition: transform 0.3s ease;
        }

        .sidebar:hover {
            transform: translateY(-5px);
            box-shadow: 
                0 15px 50px rgba(0, 0, 0, 0.6),
                0 0 80px rgba(0, 255, 204, 0.25),
                inset 0 0 50px rgba(0, 255, 204, 0.08);
        }

        .sidebar h3 {
            text-align: center;
            margin: 0 0 15px 0;
            font-size: 1.4em;
        }

        .dropdown-container {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-bottom: 15px;
        }

        .dropdown-label {
            font-size: 14px;
            color: #00ffcc;
            font-weight: 600;
            letter-spacing: 1px;
            text-align: center;
        }

        .custom-dropdown {
            position: relative;
            width: 100%;
        }

        .dropdown-selected {
            background: linear-gradient(135deg, rgba(0, 255, 204, 0.15), rgba(0, 200, 255, 0.15));
            backdrop-filter: blur(10px);
            border: 2px solid rgba(0, 255, 204, 0.5);
            border-radius: 12px;
            padding: 15px 20px;
            color: #00ffcc;
            font-weight: 700;
            font-family: 'Orbitron', sans-serif;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 255, 204, 0.2);
        }

        .dropdown-selected:hover {
            border-color: #00ffcc;
            box-shadow: 0 8px 25px rgba(0, 255, 204, 0.3);
            transform: translateY(-2px);
        }

        .dropdown-arrow {
            width: 0;
            height: 0;
            border-left: 6px solid transparent;
            border-right: 6px solid transparent;
            border-top: 6px solid #00ffcc;
            transition: transform 0.3s ease;
        }

        .dropdown-selected.active .dropdown-arrow {
            transform: rotate(180deg);
        }

        .dropdown-options {
            position: absolute;
            top: calc(100% + 10px);
            left: 0;
            right: 0;
            background: rgba(5, 5, 20, 0.95);
            backdrop-filter: blur(20px);
            border: 2px solid rgba(0, 255, 204, 0.5);
            border-radius: 12px;
            overflow: hidden;
            max-height: 0;
            opacity: 0;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
            z-index: 100;
        }

        .dropdown-options.show {
            max-height: 300px;
            opacity: 1;
        }

        .dropdown-option {
            padding: 15px 20px;
            color: #00ffcc;
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 600;
            border-bottom: 1px solid rgba(0, 255, 204, 0.1);
        }

        .dropdown-option:last-child {
            border-bottom: none;
        }

        .dropdown-option:hover {
            background: rgba(0, 255, 204, 0.2);
            padding-left: 30px;
        }

        .dropdown-option.selected {
            background: rgba(0, 255, 204, 0.15);
            font-weight: 700;
        }

        .operator-list {
            list-style: none;
            padding: 0;
            margin: 0;
            max-height: 400px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .operator-list::-webkit-scrollbar {
            width: 8px;
        }

        .operator-list::-webkit-scrollbar-track {
            background: rgba(0, 255, 204, 0.05);
            border-radius: 10px;
        }

        .operator-list::-webkit-scrollbar-thumb {
            background: rgba(0, 255, 204, 0.3);
            border-radius: 10px;
        }

        .operator-list::-webkit-scrollbar-thumb:hover {
            background: rgba(0, 255, 204, 0.5);
        }

        .operator-list li {
            padding: 16px 20px;
            border-radius: 12px;
            text-align: left;
            font-weight: 600;
            font-size: 15px;
            letter-spacing: 0.5px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            min-height: 50px;
            display: flex;
            align-items: center;
            word-wrap: break-word;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .operator-list li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 4px;
            height: 100%;
            transition: width 0.3s ease;
        }

        .operator-list li:hover::before {
            width: 100%;
            opacity: 0.1;
        }

        .operator-belum {
            background: linear-gradient(135deg, rgba(255, 77, 77, 0.7), rgba(255, 50, 50, 0.7));
            border: 2px solid rgba(255, 77, 77, 0.5);
            color: #fff;
            box-shadow: 0 5px 20px rgba(255, 77, 77, 0.4);
        }

        .operator-belum::before {
            background: rgba(255, 77, 77, 0.8);
        }

        .operator-belum:hover {
            transform: translateX(5px) scale(1.02);
            box-shadow: 0 8px 30px rgba(255, 77, 77, 0.6);
        }

        .operator-aktif {
            background: linear-gradient(135deg, rgba(0, 255, 204, 0.8), rgba(0, 220, 180, 0.8));
            border: 2px solid rgba(0, 255, 204, 0.6);
            color: #0a0014;
            box-shadow: 0 5px 20px rgba(0, 255, 204, 0.5);
        }

        .operator-aktif::before {
            background: rgba(0, 255, 204, 1);
        }

        .operator-aktif:hover {
            transform: translateX(5px) scale(1.02);
            box-shadow: 0 8px 30px rgba(0, 255, 204, 0.7);
        }

        .withdraw-op {
            background: linear-gradient(135deg, rgba(0, 200, 255, 0.85), rgba(0, 180, 255, 0.85));
            border: 2px solid rgba(0, 200, 255, 0.6);
            color: #0a0014;
            box-shadow: 0 5px 20px rgba(0, 200, 255, 0.5);
            animation: withdrawGlow 2s ease-in-out infinite alternate;
        }

        .withdraw-op::before {
            background: rgba(0, 200, 255, 1);
        }

        @keyframes withdrawGlow {
            from {
                box-shadow: 0 5px 20px rgba(0, 200, 255, 0.5);
            }
            to {
                box-shadow: 0 8px 35px rgba(0, 200, 255, 0.8);
            }
        }

        .withdraw-op:hover {
            transform: translateX(5px) scale(1.02);
            animation: none;
            box-shadow: 0 8px 30px rgba(0, 200, 255, 0.7);
        }

        .small {
            font-size: 13px;
            color: rgba(170, 170, 255, 0.8);
            text-align: center;
            margin: 10px 0;
            line-height: 1.5;
            padding: 12px;
            background: rgba(0, 0, 0, 0.3);
            border-radius: 10px;
            border-left: 3px solid rgba(0, 200, 255, 0.5);
        }

        .search {
            width: 100%;
            padding: 12px 16px;
            border-radius: 12px;
            margin: 10px 0;
            border: 2px solid rgba(0, 255, 204, 0.3);
            background: rgba(5, 5, 20, 0.7);
            backdrop-filter: blur(10px);
            color: #00ffcc;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.3s ease;
            box-shadow: inset 0 0 15px rgba(0, 255, 204, 0.1);
        }

        .search:focus {
            outline: none;
            border-color: #00ffcc;
            box-shadow: 
                inset 0 0 20px rgba(0, 255, 204, 0.2),
                0 0 20px rgba(0, 255, 204, 0.3);
        }

        .search::placeholder {
            color: rgba(0, 255, 204, 0.4);
        }

        @media (max-width: 1200px) {
            .container {
                flex-direction: column;
            }

            .sidebar {
                width: 100%;
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .main, .sidebar {
            animation: fadeIn 0.6s ease-out;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="main">
            <h1>VALIDASI KOIN TVTOTO</h1>
            
            <textarea id="dataInput" placeholder="Masukkan data transaksi (paste langsung dari Excel/Sheets; setiap baris satu transaksi)"></textarea>
            
            <div class="button-group">
                <button onclick="tambahData()">➕ Tambah Data</button>
                <button onclick="hapusData()">🗑️ Hapus Semua Data</button>
                <button onclick="hitungJumlahDanTotalTransaksi()">📊 Hitung Jumlah & Total</button>
                <button onclick="downloadCSV()">💾 Download CSV</button>
            </div>

            <h2>📑 Hasil Rekap:</h2>
            <div id="hasil"></div>
        </div>

        <div class="sidebar">
            <h3>📌 Referensi Operator</h3>
            <textarea id="refInput" placeholder="Tempel data operator (kolom deposit & withdraw) di sini..."></textarea>
            <button onclick="setReferensi()">💾 Simpan Referensi</button>

            <div class="small">
                <strong>ℹ️ Info:</strong> Script akan otomatis membaca baris yang berisi kata <strong>Withdraw</strong> sebagai data withdraw (langsung ke rekap withdraw).
            </div>

            <div class="dropdown-container">
                <div class="dropdown-label">🔍 Filter Operator Berdasarkan Tipe:</div>
                <div class="custom-dropdown">
                    <div class="dropdown-selected" onclick="toggleDropdown()">
                        <span id="selectedFilter">Semua Operator</span>
                        <div class="dropdown-arrow"></div>
                    </div>
                    <div class="dropdown-options" id="dropdownOptions">
                        <div class="dropdown-option selected" onclick="selectFilter('all')">Semua Operator</div>
                        <div class="dropdown-option" onclick="selectFilter('deposit')">Hanya Deposit</div>
                        <div class="dropdown-option" onclick="selectFilter('withdraw')">Hanya Withdraw</div>
                    </div>
                </div>
            </div>

            <input id="withdrawSearch" class="search" placeholder="🔎 Cari nama operator..." oninput="filterWithdrawList()" />

            <h3>👤 Daftar Operator</h3>
            <ul id="operatorList" class="operator-list"></ul>

            <h3>🏦 Operator Withdraw</h3>
            <ul id="withdrawList" class="operator-list"></ul>
        </div>
    </div>

    <script>
        let dataTransaksi = [];
        let dataWithdraw = [];
        let daftarOperator = new Set();
        let operatorReferensi = {};
        let operatorWithdraw = {};
        let currentFilter = 'all';

        function tokenizeLine(line){
            line = line.replace(/\u00A0/g,' ').trim();
            if(!line) return [];
            let tokens = line.split('\t');
            if(tokens.length === 1){
                tokens = line.split(',');
            }
            if(tokens.length === 1){
                tokens = line.split(/\s{2,}/);
            }
            if(tokens.length === 1){
                tokens = line.split(/\s+/);
            }
            tokens = tokens.map(t => t.replace(/^"|"$/g,'').trim());
            return tokens;
        }

        function tambahData(){
            const dataInput = document.getElementById('dataInput').value;
            if(!dataInput.trim()){ alert('Tidak ada data untuk ditambahkan.'); return; }
            const dataLines = dataInput.trim().split('\n');

            dataLines.forEach(line=>{
                const tokens = tokenizeLine(line);
                if(!tokens.length) return;

                const lower = tokens.map(t => (t||'').toString().toLowerCase());
                const isWithdraw = lower.some(t => t === 'withdraw' || t.includes('withdraw'));

                if(isWithdraw){
                    const row = new Array(12).fill('');
                    for(let i=0;i<Math.min(tokens.length,12);i++){
                        row[i] = tokens[i];
                    }
                    if(!row[10] || !/withdraw/i.test(row[10])) row[10] = 'Withdraw';
                    dataWithdraw.push(row);

                    const operatorName = (row[9]||row[0]||'').toString().trim();
                    if(operatorName){
                        if(operatorReferensi[operatorName]) operatorReferensi[operatorName].hasWithdraw = true;
                        daftarOperator.add(operatorName);
                    }
                } else {
                    const row = new Array(12).fill('');
                    for(let i=0;i<Math.min(tokens.length,12);i++){
                        row[i] = tokens[i];
                    }
                    if(!/[\d]/.test((row[2]||'')) ){
                        const found = tokens.find(t => /[0-9]/.test(t));
                        if(found) row[2] = found;
                    }
                    dataTransaksi.push(row);
                    const operatorName = (row[9]||row[1]||row[0]||'').toString().trim();
                    if(operatorName){
                        if(operatorReferensi[operatorName]) operatorReferensi[operatorName].status = true;
                        daftarOperator.add(operatorName);
                    }
                }
            });

            document.getElementById('dataInput').value='';
            updateOperatorList();
            alert('✅ Data berhasil disimpan! (Deposit + Withdraw)');
        }

        function hapusData(){
            dataTransaksi = [];
            dataWithdraw = [];
            daftarOperator.clear();
            document.getElementById('hasil').innerHTML = '';
            document.getElementById('operatorList').innerHTML = '';
            document.getElementById('withdrawList').innerHTML = '';
            for(const op in operatorReferensi){
                operatorReferensi[op].status = false;
                operatorReferensi[op].hasWithdraw = false;
            }
            operatorWithdraw = {};
            alert('🗑 Semua data dihapus!');
        }

        function hitungJumlahDanTotalTransaksi(){
            const hasilDeposit = {};
            dataTransaksi.forEach(item=>{
                const bankPembayaran = (item[3]||'Unknown').toString() || 'Unknown';
                const jumlah = parseInt((item[2]||'0').toString().replace(/[^0-9\-]/g,''),10) || 0;
                if(!hasilDeposit[bankPembayaran]) hasilDeposit[bankPembayaran] = { total:0, count:0 };
                hasilDeposit[bankPembayaran].total += jumlah;
                hasilDeposit[bankPembayaran].count++;
            });

            const hasilWithdraw = {};
            dataWithdraw.forEach(item=>{
                const bankPenerima = (item[6]||'Unknown').toString() || 'Unknown';
                const jumlah = parseInt((item[2]||'0').toString().replace(/[^0-9\-]/g,''),10) || 0;
                if(!hasilWithdraw[bankPenerima]) hasilWithdraw[bankPenerima] = { total:0, count:0 };
                hasilWithdraw[bankPenerima].total += jumlah;
                hasilWithdraw[bankPenerima].count++;
            });

            let totalGabungan = 0;
            for(const b in hasilDeposit) totalGabungan += hasilDeposit[b].total;
            for(const b in hasilWithdraw) totalGabungan += hasilWithdraw[b].total;

            let hasilOutput = `
            <h3>📌 Rekap Deposit</h3>
            <table><thead><tr><th>Bank</th><th>Jumlah Transaksi</th><th>Total</th></tr></thead><tbody>`;
            for(const bank in hasilDeposit){
                hasilOutput += `<tr><td>${bank}</td><td>${hasilDeposit[bank].count}</td><td>${hasilDeposit[bank].total.toLocaleString()}</td></tr>`;
            }
            hasilOutput += '</tbody></table>';

            hasilOutput += `
            <h3>📌 Rekap Withdraw</h3>
            <table><thead><tr><th>Bank</th><th>Jumlah Transaksi</th><th>Total</th></tr></thead><tbody>`;
            for(const bank in hasilWithdraw){
                hasilOutput += `<tr><td>${bank}</td><td>${hasilWithdraw[bank].count}</td><td>${hasilWithdraw[bank].total.toLocaleString()}</td></tr>`;
            }
            hasilOutput += '</tbody></table>';

            hasilOutput += `<div style="margin-top:15px;text-align:center;font-weight:bold;font-size:1.2em;color:#00ffcc;text-shadow:0 0 10px rgba(0,255,204,0.6);">💰 Total Gabungan (Deposit + Withdraw): ${totalGabungan.toLocaleString()}</div>`;

            document.getElementById('hasil').innerHTML = hasilOutput;
        }

        function downloadCSV(){
            if(!dataTransaksi.length && !dataWithdraw.length){ alert('Tidak ada data transaksi untuk di-download.'); return; }
            let csv = "Akun Pemain,Tanggal,Jumlah,Bank Pembayaran,No Rek Pembayar,Nama Rek Pembayar,Bank Penerima,No Rek Penerima,Nama Rek Penerima,Akun Operator,Jenis Operasi,Status\n";
            [...dataTransaksi, ...dataWithdraw].forEach(row => {
                csv += row.map(v => `"${(v||'').toString().replace(/"/g,'""')}"`).join(",") + "\n";
            });
            const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement('a');
            link.href = URL.createObjectURL(blob);
            link.download = 'transaksi.csv';
            link.click();
        }

        function setReferensi(){
            const text = document.getElementById('refInput').value.trim();
            if(!text){ alert('Tidak ada referensi untuk disimpan.'); return; }
            const lines = text.split('\n');
            operatorReferensi = {};
            operatorWithdraw = {};
            lines.forEach((rawLine, idx)=>{
                let line = rawLine.replace(/\u00A0/g,' ').trim();
                if(!line) return;
                if(idx === 0 && /operator|deposit|withdraw|no/i.test(line)) return;

                const tokens = line.split(/\s+/);
                let opIdx = tokens.findIndex(t => /[A-Za-z]/.test(t));
                if(opIdx === -1) opIdx = 0;
                let nameParts = [];
                for(let j=opIdx;j<tokens.length;j++){
                    if(/^[0-9\-,.]+$/.test(tokens[j])) break;
                    nameParts.push(tokens[j]);
                }
                const nama = nameParts.join(' ').replace(/[^A-Za-z0-9_\- ]/g,'').trim();
                if(!nama) return;

                const numeric = tokens.slice(opIdx + nameParts.length).map(t=>parseInt(t.replace(/[^0-9\-]/g,''),10)||0);
                let depositVal = numeric[0]||0;
                let withdrawVal = numeric[1]||0;

                operatorReferensi[nama] = { status:false, hasWithdraw: withdrawVal>0 };
                if(withdrawVal>0) operatorWithdraw[nama] = withdrawVal;
            });
            updateWithdrawList();
            updateOperatorList();
            alert('✅ Referensi operator berhasil disimpan! Ditemukan: ' + Object.keys(operatorReferensi).length + ' operator.');
        }

        function updateOperatorList(){
            const list = document.getElementById('operatorList');
            list.innerHTML = '';
            const keys = Object.keys(operatorReferensi).sort((a,b)=>a.localeCompare(b));
            if(!keys.length){
                const li = document.createElement('li');
                li.textContent = '— Belum ada referensi deposit —';
                li.className='operator-belum';
                list.appendChild(li);
                return;
            }
            keys.forEach(op=>{
                const li = document.createElement('li');
                li.textContent = op;
                
                if(currentFilter === 'deposit' && operatorReferensi[op].hasWithdraw && !operatorReferensi[op].status) {
                    li.style.display = 'none';
                } else if(currentFilter === 'withdraw' && !operatorReferensi[op].hasWithdraw) {
                    li.style.display = 'none';
                }
                
                if(operatorReferensi[op].status) li.className = 'operator-aktif';
                else if(operatorReferensi[op].hasWithdraw) li.className = 'withdraw-op';
                else li.className = 'operator-belum';
                list.appendChild(li);
            });
        }

        function updateWithdrawList(){
            const list = document.getElementById('withdrawList');
            list.innerHTML = '';
            const names = Object.keys(operatorWithdraw).sort((a,b)=>a.localeCompare(b));
            if(!names.length){
                const li = document.createElement('li');
                li.textContent = '— Tidak ada operator dengan nominal withdraw —';
                li.className='operator-belum';
                list.appendChild(li);
                return;
            }
            names.forEach(name=>{
                const li = document.createElement('li');
                li.textContent = name;
                li.className = 'withdraw-op';
                list.appendChild(li);
            });
            filterWithdrawList();
        }

        function filterWithdrawList(){
            const q = (document.getElementById('withdrawSearch').value || '').toLowerCase().trim();
            const list = document.getElementById('withdrawList');
            Array.from(list.children).forEach(li=>{
                const txt = li.textContent.toLowerCase();
                li.style.display = q ? (txt.includes(q) ? '' : 'none') : '';
            });
        }

        function toggleDropdown() {
            const dropdown = document.getElementById('dropdownOptions');
            const selected = document.querySelector('.dropdown-selected');
            dropdown.classList.toggle('show');
            selected.classList.toggle('active');
        }

        function selectFilter(filter) {
            currentFilter = filter;
            const options = document.querySelectorAll('.dropdown-option');
            options.forEach(opt => opt.classList.remove('selected'));
            
            let selectedText = '';
            if(filter === 'all') {
                selectedText = 'Semua Operator';
                options[0].classList.add('selected');
            } else if(filter === 'deposit') {
                selectedText = 'Hanya Deposit';
                options[1].classList.add('selected');
            } else if(filter === 'withdraw') {
                selectedText = 'Hanya Withdraw';
                options[2].classList.add('selected');
            }
            
            document.getElementById('selectedFilter').textContent = selectedText;
            toggleDropdown();
            updateOperatorList();
        }

        document.addEventListener('click', function(e) {
            const dropdown = document.querySelector('.custom-dropdown');
            if (!dropdown.contains(e.target)) {
                document.getElementById('dropdownOptions').classList.remove('show');
                document.querySelector('.dropdown-selected').classList.remove('active');
            }
        });

        updateOperatorList();
        updateWithdrawList();
    </script>
</body>
</html>
